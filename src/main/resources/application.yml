spring:
  ai:
    anthropic:
      api-key: ${ANTHROPIC_API_KEY:demo}
      default-options:
        temperature: 0.7
        max-tokens: 1000
        model: claude-3-haiku-20240307
    embedding:
      default-model: anthropic
      anthropic:
        api-key: ${ANTHROPIC_API_KEY:demo}
        options:
          model: claude-3-haiku-20240307 